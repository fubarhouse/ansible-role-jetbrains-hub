---

- hosts: all
  sudo: true
  pre_tasks:
    - name: install sudo
      package:
        name: sudo
        state: present
      failed_when: false
  roles:
    - geerlingguy.git
    - fubarhouse.commons
    - fubarhouse.curl
    - role_under_test